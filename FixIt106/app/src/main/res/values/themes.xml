<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.fixit" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary -->
        <item name="colorPrimary">@color/brand_primary</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary -->
        <item name="colorSecondary">@color/brand_secondary</item>
        <item name="colorOnSecondary">@color/gray_900</item>

        <!-- Surfaces -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <!-- FirebaseUI 7.x is a Material2 library and reads colorAccent for button tinting -->
        <item name="colorAccent">@color/brand_primary</item>

        <!-- Optional: rounded UI feeling -->
        <item name="shapeAppearanceCornerMedium">?attr/shapeAppearanceCornerExtraLarge</item>
    </style>

</resources>